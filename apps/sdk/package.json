{
  "name": "@director.run/sdk",
  "type": "module",
  "version": "0.0.4",
  "files": ["dist", "README.md"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/director-run/director"
  },
  "description": "Director SDK for managing MCP servers programmatically",
  "author": "The Working Company <hello@director.run>",
  "homepage": "https://director.run",
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "clean": "rm -rf .turbo dist/*",
    "test": "echo 'no tests'",
    "typecheck": "tsc --noEmit",
    "build": "tsup && api-extractor run --local || true && if [ -f dist/index.bundled.d.ts ]; then mv dist/index.bundled.d.ts dist/index.d.ts && cp dist/index.d.ts dist/index.d.cts; fi",
    "release": "bun publish --access public --omit=peer",
    "prepare": "bun run build",
    "dev": "bun --watch bin/cli.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "devDependencies": {
    "@director.run/gateway": "workspace:*",
    "@director.run/registry": "workspace:*",
    "@microsoft/api-extractor": "^7.52.8",
    "rollup": "^4.45.1",
    "rollup-plugin-dts": "^6.2.1",
    "tsup": "^8.5.0"
  }
}
