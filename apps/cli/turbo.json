{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@director.run/core#build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["@director.run/core#build"],
      "outputs": ["dist/**"]
    }
  }
}
