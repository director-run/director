import { describe, it } from "vitest";

describe("parseParameters", () => {
  it("should parse parameters", () => {
    const entry = {
      transport: { type: "stdio", args: ["--foo", "--bar"] },
    };
  });
});
